/* eslint-disable prettier/prettier */
export const config = {
    appName: "hr-portal",
    port: 3000,
    db: {
        url: "mongodb://localhost/hr_portal",
        debug: true,
        syncIndex: true
    }
}