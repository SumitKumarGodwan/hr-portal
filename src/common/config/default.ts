/* eslint-disable prettier/prettier */
export const config = {
    appName: "hr-portal",
    port: 3000,
    db: {
        driver: "mongodb",
        host: ["localhost"],
        port: 27017,
        database: "hr-portal",
    }
}